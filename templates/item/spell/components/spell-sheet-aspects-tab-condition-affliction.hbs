{{! Condition removal }}
<div class="spell-sheet-aspects-tab-condition-affliction stat-category double-column">

  {{! Category label }}
  <h3 class="stat-category-label">{{localize "TITAN.conditionAffliction.label"}}</h3>

  <div class="double-column-columns">
    {{! Blinded }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.blinded.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.blinded" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.blinded}}>
    </div>

    {{! Charmed }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.charmed.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.charmed" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.charmed}}>
    </div>

    {{! Deafened }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.deafened.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.deafened" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.deafened}}>
    </div>

    {{! Frightened }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.frightened.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.frightened" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.frightened}}>
    </div>

    {{! Incapacitated }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.incapacitated.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.incapacitated" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.incapacitated}}>
    </div>

    {{! Poisoned }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.poisoned.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.poisoned" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.poisoned}}>
    </div>

    {{! Prone }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.prone.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.prone" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.prone}}>
    </div>

    {{! Restrained }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.restrained.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.restrained" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.restrained}}>
    </div>

    {{! Stunned }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.stunned.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.stunned" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.stunned}}>
    </div>

    {{! Unconscious }}
    <div class="stat-field-horizontal">
      {{! Label }}
      <div class="stat-label">
        {{localize "TITAN.condition.option.unconscious.label"}}
      </div>

      {{! Checkbox }}
      <input name="system.conditionAffliction.unconscious" class="stat-input" type="checkbox" {{checked
        system.conditionAffliction.unconscious}}>
    </div>
  </div>
</div>